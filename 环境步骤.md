# docker 环境配置步骤

1. 创建`Dockerfile`文件
2. `docker build -t corecoder .`
3. ```Python
   docker run -it --rm -e PYTHONUTF8=1 --env-file .env -v "D:\Desktop\AI\CoreCoder:/corecoder" corecoder -p "读 corecoder/agent.py，告诉我循环最多转多少轮"
   ```
4. 测试环境

```Python
docker run --rm -v "D:\Desktop\AI\CoreCoder:/corecoder" --entrypoint python corecoder -m pytest tests/ -q
```
